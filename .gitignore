# Credentials and per-environment configuration
.env
.env.*
!.env.*.example

# Persistent Docker data (local and production)
/data/

# This is an independent Git repository with its own origin and upstream.
/cosmonauta_theme/

# Editor and OS metadata
.DS_Store
.idea/
.vscode/
